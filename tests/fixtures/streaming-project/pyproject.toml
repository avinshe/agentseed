[project]
name = "streaming-demo"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "confluent-kafka>=2.3",
    "apache-flink>=1.18",
    "prefect>=2.14",
    "jupyter>=1.0",
    "soda-core>=3.0",
]
